name: crDroid SM8550 5.15 Kernel Build Process
permissions:
  contents: write
  actions: write 

on:
  workflow_dispatch:
    inputs:
      susfs:
        description: 'Включить SUSFS?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      bbg:
        description: 'Включить Baseband-guard?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      unicodefix:
        description: 'Включить Unicode fix?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      zeromount:
        description: 'Включить Zeromount?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      wksu:
        description: 'Включить KernelSU-Next (wksu)?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      droidspace:
        description: 'Включить поддержку Droidspace?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      ntsync:
        description: 'Включить NTsync?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'
      optimizations:
        description: 'Включить оптимизации?'
        required: true
        type: choice
        options: ['true', 'false']
        default: 'true'

jobs:
  build-crdroid-sm8550:
    name: "crDroid 16.0 - Kernel 5.15"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Free Disk Space
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true
          remove_swap: true
          remove_packages: "azure-cli google-cloud-cli microsoft-edge-stable google-chrome-stable firefox postgresql* temurin-* *llvm* mysql* dotnet-sdk-*"
          remove_packages_one_command: true
          remove_folders: "/usr/share/swift /usr/share/miniconda /usr/share/az* /usr/local/lib/node_modules /usr/local/share/chromium /usr/local/share/powershell /usr/local/julia /usr/local/aws-cli /usr/local/aws-sam-cli /usr/share/gradle"
          rm_cmd: "rmz"
          rmz_version: "3.1.1"
      
      - name: Set ENV Variables
        run: |
          echo "CONFIG=16.0-16-5.15" >> $GITHUB_ENV

      - name: Clone AnyKernel3 and Dependencies
        run: |
          git clone https://github.com/WildKernels/AnyKernel3.git -b "gki-2.0"
          git clone https://github.com/WildKernels/kernel_patches.git
          # Используем правильную ветку для ядра 5.15
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b "gki-android13-5.15"
          git clone https://github.com/jimsterino98/Super-Builders.git
      
      - name: Clone crDroid Kernel Source
        run: |
            git clone --depth 1 -b 16.0 --single-branch https://github.com/crdroidandroid/android_kernel_samsung_sm8550.git $CONFIG
            
      - name: Set Path Environment Variables
        run: |
          ROOT="$GITHUB_WORKSPACE"
          SUSFS4KSU="$ROOT/susfs4ksu"
          KERNEL_PATCHES="$ROOT/kernel_patches"
          ANYKERNEL3="$ROOT/AnyKernel3"
          ZEROMOUNT="$ROOT/Super-Builders"
          COMMON="$CONFIG"
          GKI_DEFCONFIG="$COMMON/arch/arm64/configs/gki_defconfig"

          echo "ROOT=$ROOT" >> $GITHUB_ENV
          echo "SUSFS4KSU=$SUSFS4KSU" >> $GITHUB_ENV
          echo "KERNEL_PATCHES=$KERNEL_PATCHES" >> $GITHUB_ENV
          echo "ANYKERNEL3=$ANYKERNEL3" >> $GITHUB_ENV
          echo "COMMON=$COMMON" >> $GITHUB_ENV
          echo "GKI_DEFCONFIG=$GKI_DEFCONFIG" >> $GITHUB_ENV
          echo "ZEROMOUNT=$ZEROMOUNT" >> $GITHUB_ENV

      - name: Add KernelSU-Next
        if: inputs.wksu == 'true'
        run: |
          cd "$COMMON"
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s stable
          echo "CONFIG_KSU=y" >> arch/arm64/configs/gki_defconfig

      - name: Getting KernelSU-Next Version
        if: inputs.wksu == 'true'
        run: |
          cd "$COMMON/KernelSU-Next/kernel"
          KBUILD="Kbuild"
          KSU_VERSION=$((30000 + $(git rev-list --count HEAD 2>/dev/null || echo 3129)))
          KSU_GIT_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v3.2.0")
          echo "KSU_VERSION=$KSU_VERSION" >> $GITHUB_ENV
          sed -i "s/KSU_VERSION_FALLBACK := [0-9]\+/KSU_VERSION_FALLBACK := ${KSU_VERSION}/" "$KBUILD"
          sed -i "s/KSU_VERSION_TAG_FALLBACK := .*/KSU_VERSION_TAG_FALLBACK := ${KSU_GIT_TAG}/" "$KBUILD"

      - name: Apply SUSFS Patches
        if: inputs.susfs == 'true'
        run: |
          cd "$COMMON/KernelSU-Next"
          cp "$SUSFS4KSU/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" .
          patch -p1 < 10_enable_susfs_for_ksu.patch || true

          cp "$KERNEL_PATCHES/wild/ksun-0daf55582-susfs-v2.1.0-e259e7d6.patch" .
          patch -p1 < ksun-0daf55582-susfs-v2.1.0-e259e7d6.patch || true
           
          cd ..
          cp "$SUSFS4KSU/kernel_patches/fs/"* ./fs/
          cp "$SUSFS4KSU/kernel_patches/include/linux/"* ./include/linux/
          
          # Находим патч 50_add_susfs для 5.15 в скачанной ветке
          cp "$SUSFS4KSU/kernel_patches/50_add_susfs_in_gki-android13-5.15.patch" ./ || cp "$SUSFS4KSU/kernel_patches/50_add_susfs_in_gki-5.15"* ./

          sed -i '/#ifdef CONFIG_SECURITY_DEFEX/,/^#endif/d' fs/open.c
          sed -i '/copy_flags = CL_COPY_UNBINDABLE | CL_EXPIRE;/,/#endif/ { /#ifdef CONFIG_KDP_NS/,/#endif/ { /#else/,/#endif/!d; /#else/d; /#endif/d } }' fs/namespace.c
          
          patch -p1 < 50_add_susfs_in_gki-*.patch || true
          echo "CONFIG_KSU_SUSFS=y" >> arch/arm64/configs/gki_defconfig
          
          if [[ ${{ inputs.zeromount }} == 'true' ]]; then
            cp "$ZEROMOUNT/15-5.15/WildKSU/patches/60_zeromount-15-5.15.patch" .
            patch -p1 < 60_zeromount-15-5.15.patch || true
            echo "CONFIG_ZEROMOUNT=y" >> arch/arm64/configs/gki_defconfig
          fi
          
      - name: Add BBG
        if: inputs.bbg == 'true'
        run: |          
          cd "$COMMON"
          curl -LSs https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          echo "CONFIG_BBG=y" >> arch/arm64/configs/gki_defconfig
          sed -i '/^CONFIG_LSM=/ s/"$/,baseband_guard"/' arch/arm64/configs/gki_defconfig

      - name: Add unicodefix
        if: inputs.unicodefix == 'true'
        run: |  
          cd "$COMMON"
          cp "$KERNEL_PATCHES/common/unicode_bypass_fix_6.1-.patch" .
          patch -p1 < unicode_bypass_fix_6.1-.patch || true

      - name: Add droidspace support
        if: inputs.droidspace == 'true'
        run: |  
          cd "$COMMON"
          curl -Ls https://github.com/nullptr-t-oss/android_kernel_common_oneplus_sm8550/commit/769a5cfdc17b98d8e0bd491d45f5651180e42196.patch | patch -p1 --forward || true
          cat >> arch/arm64/configs/gki_defconfig << 'EOF'
          CONFIG_SYSVIPC=y
          CONFIG_POSIX_MQUEUE=y
          CONFIG_IPC_NC=y
          CONFIG_PID_NS=y
          CONFIG_DEVTMPFS=y
          EOF
          
      - name: Build Kernel
        run : |
          cd "$CONFIG"
          if [ -f "tools/bazel" ]; then
            tools/bazel build --config=fast --lto=thin //common:kernel_aarch64_dist
          else
            echo "Bazel not found. Check crDroid build system."
            exit 1
          fi
          
      - name: Prepare AnyKernel3
        run: |
          cd "$CONFIG"
          find ./bazel-bin -name Image -exec cp {} "$ANYKERNEL3/Image" \; || cp ./out/dist/Image "$ANYKERNEL3/Image"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v6
        with:
          name: KSU-crDroid-SM8550-5.15
          path: ./AnyKernel3/**
